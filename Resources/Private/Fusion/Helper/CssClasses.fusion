prototype(TechDivision.NodeTypes.FlexColumnLayouts:Helper.CssClasses) < prototype(Neos.Fusion:DataStructure) {
    @process {
        removeEmpty = ${Array.filter(value, (val) => val != '' && val != null)}
        join = ${Array.join(value, ' ')}
    }
}
